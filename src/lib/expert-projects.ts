// Expert-level projects — premium, in-depth, real-world AWS architectures
// Added in v2 to take the platform from intermediate to true expert depth.

import type { Project } from "./learning-content";

export const expertProjects: Project[] = [
  {
    id: "project-6-multi-region-serverless",
    title: "Project 6: Multi-Region Serverless API with Active-Active Failover",
    level: "expert",
    estimatedHours: 8,
    cost: "~$80-150/month at moderate traffic (Route 53 + Lambda + DynamoDB Global Tables + CloudFront)",
    description: "Build a globally distributed serverless API that survives a complete AWS region failure with <60 second RTO and zero data loss. This is the architecture that powers real production systems at companies like Netflix, Airbnb, and Lyft — and it's the kind of design senior solutions architects are expected to deliver on AWS.",
    objectives: [
      "Deploy identical serverless stacks (API Gateway + Lambda + DynamoDB) in 2 AWS regions",
      "Configure DynamoDB Global Tables for bi-directional multi-region replication",
      "Use Route 53 latency-based routing with health checks for automatic failover",
      "Implement CloudFront as a global edge layer with origin failover",
      "Build a CI/CD pipeline that deploys to both regions in parallel",
      "Test failover by simulating a regional outage and measuring RTO/RPO",
      "Implement idempotent Lambda handlers safe for retries during failover",
    ],
    architecture: {
      nodes: [
        { id: "user", label: "Global Users", type: "client" },
        { id: "r53", label: "Route 53 (Latency Routing)", type: "edge" },
        { id: "cf", label: "CloudFront (Global Edge)", type: "edge" },
        { id: "apigw1", label: "API Gateway (us-east-1)", type: "edge" },
        { id: "apigw2", label: "API Gateway (eu-west-1)", type: "edge" },
        { id: "lam1", label: "Lambda (us-east-1)", type: "compute" },
        { id: "lam2", label: "Lambda (eu-west-1)", type: "compute" },
        { id: "ddb1", label: "DynamoDB (us-east-1)", type: "database" },
        { id: "ddb2", label: "DynamoDB (eu-west-1)", type: "database" },
        { id: "gt", label: "Global Tables Replication", type: "network" },
      ],
      edges: [
        { from: "user", to: "r53", label: "DNS lookup" },
        { from: "r53", to: "cf", label: "lowest latency" },
        { from: "cf", to: "apigw1", label: "primary origin" },
        { from: "cf", to: "apigw2", label: "failover origin" },
        { from: "apigw1", to: "lam1" },
        { from: "apigw2", to: "lam2" },
        { from: "lam1", to: "ddb1" },
        { from: "lam2", to: "ddb2" },
        { from: "ddb1", to: "gt", label: "bi-directional sync" },
        { from: "gt", to: "ddb2" },
      ],
    },
    steps: [
      {
        title: "Step 1: Design the architecture and choose regions",
        description: "Pick two regions at least 2,500 km apart for true disaster resilience. Common pairs: us-east-1 (Virginia) + eu-west-1 (Ireland), or us-west-2 (Oregon) + ap-southeast-1 (Singapore). Verify both regions support all services you need.",
        command: [
          "# Verify service availability in both regions",
          "for REGION in us-east-1 eu-west-1; do",
          "  echo \"=== $REGION ===\"",
          "  aws lambda list-functions --region $REGION --query 'Functions | length(@)'",
          "  aws dynamodb list-tables --region $REGION --query 'TableNames | length(@)'",
          "done",
        ].join("\n"),
      },
      {
        title: "Step 2: Create the DynamoDB Global Table",
        description: "First create the table in the primary region, then replicate it. Global Tables require the table to be empty when first added to a global table, OR you can create the table in each region first then add to global.",
        command: [
          "# Create table in us-east-1",
          "aws dynamodb create-table \\",
          "  --table-name Orders \\",
          "  --attribute-definitions AttributeName=orderId,AttributeType=S AttributeName=customerId,AttributeType=S \\",
          "  --key-schema AttributeName=orderId,KeyType=HASH AttributeName=customerId,KeyType=RANGE \\",
          "  --billing-mode PAY_PER_REQUEST \\",
          "  --region us-east-1",
          "",
          "# Wait for active",
          "aws dynamodb wait table-exists --table-name Orders --region us-east-1",
          "",
          "# Create identical table in eu-west-1 (must have same schema)",
          "aws dynamodb create-table \\",
          "  --table-name Orders \\",
          "  --attribute-definitions AttributeName=orderId,AttributeType=S AttributeName=customerId,AttributeType=S \\",
          "  --key-schema AttributeName=orderId,KeyType=HASH AttributeName=customerId,KeyType=RANGE \\",
          "  --billing-mode PAY_PER_REQUEST \\",
          "  --region eu-west-1",
          "",
          "# Create global table (uses new API)",
          "aws dynamodb create-global-table \\",
          "  --global-table-name Orders \\",
          "  --replication-group RegionName=us-east-1 RegionName=eu-west-1 \\",
          "  --region us-east-1",
        ].join("\n"),
        warning: "DynamoDB Global Tables replicate asynchronously with typically sub-second latency, but conflicts can occur on concurrent writes to the same item. Design with last-writer-wins (using timestamp attributes) or use conditional writes for critical updates.",
      },
      {
        title: "Step 3: Write idempotent Lambda handlers",
        description: "When API Gateway retries during failover, your Lambda may execute the same request twice. Use orderId as an idempotency key — write a PutItem with a condition expression that fails if the item already exists. This is the difference between 'correct under normal conditions' and 'correct during a regional disaster'.",
        command: [
          "// handler.mjs (Lambda handler with idempotency)",
          "import { DynamoDBClient } from '@aws-sdk/client-dynamodb';",
          "import { DynamoDBDocumentClient, PutCommand } from '@aws-sdk/lib-dynamodb';",
          "",
          "const client = new DynamoDBClient({});",
          "const doc = DynamoDBDocumentClient.from(client);",
          "",
          "export const handler = async (event) => {",
          "  const body = JSON.parse(event.body);",
          "  const orderId = body.orderId; // client-supplied idempotency key",
          "  const now = Date.now();",
          "",
          "  try {",
          "    await doc.send(new PutCommand({",
          "      TableName: process.env.TABLE_NAME,",
          "      Item: {",
          "        orderId,",
          "        customerId: body.customerId,",
          "        amount: body.amount,",
          "        status: 'created',",
          "        createdAt: now,",
          "        region: process.env.AWS_REGION, // record which region processed",
          "      },",
          "      ConditionExpression: 'attribute_not_exists(orderId)', // idempotency!",
          "    }));",
          "    return { statusCode: 201, body: JSON.stringify({ orderId }) };",
          "  } catch (e) {",
          "    if (e.name === 'ConditionalCheckFailedException') {",
          "      // Already processed — return success (idempotent)",
          "      return { statusCode: 200, body: JSON.stringify({ orderId, message: 'already exists' }) };",
          "    }",
          "    throw e; // real error",
          "  }",
          "};",
        ].join("\n"),
      },
      {
        title: "Step 4: Deploy identical stacks to both regions using SAM",
        description: "Use AWS SAM with a parameterized template. Deploy the same template twice with different region parameters. This ensures both regions are bit-for-bit identical — critical for failover to actually work.",
        command: [
          "# Deploy to us-east-1",
          "sam deploy --region us-east-1 \\",
          "  --stack-name orders-api-prod \\",
          "  --parameter-overrides TableName=orders Region=us-east-1 \\",
          "  --capabilities CAPABILITY_IAM --no-fail-on-empty-changeset",
          "",
          "# Deploy to eu-west-1 (same template, different region)",
          "sam deploy --region eu-west-1 \\",
          "  --stack-name orders-api-prod \\",
          "  --parameter-overrides TableName=orders Region=eu-west-1 \\",
          "  --capabilities CAPABILITY_IAM --no-fail-on-empty-changeset",
        ].join("\n"),
      },
      {
        title: "Step 5: Configure Route 53 latency-based routing with health checks",
        description: "Create health checks for each regional API endpoint, then create a latency-based routing policy that fails over to the healthy region within 60 seconds.",
        command: [
          "# Create health checks for each regional API endpoint",
          "aws route53 create-health-check --caller-reference hc-useast-$(date +%s) \\",
          "  --health-check-config Type=HTTPS,ResourcePath=/health,FullyQualifiedDomainName=api-us.example.com,RequestInterval=30,FailureThreshold=2",
          "# Returns HealthCheckId — note it",
          "",
          "aws route53 create-health-check --caller-reference hc-euwest-$(date +%s) \\",
          "  --health-check-config Type=HTTPS,ResourcePath=/health,FullyQualifiedDomainName=api-eu.example.com,RequestInterval=30,FailureThreshold=2",
          "",
          "# Create latency-based record set with failover",
          "# (apply via JSON file due to complexity)",
          "cat > routing-policy.json <<EOF",
          "{",
          "  \"Comment\": \"Multi-region failover for orders API\",",
          "  \"Changes\": [{",
          "    \"Action\": \"CREATE\",",
          "    \"ResourceRecordSet\": {",
          "      \"Name\": \"api.example.com.\",",
          "      \"Type\": \"CNAME\",",
          "      \"SetIdentifier\": \"us-east-1-primary\",",
          "      \"Region\": \"us-east-1\",",
          "      \"HealthCheckId\": \"<us-east-hc-id>\",",
          "      \"AliasTarget\": {",
          "        \"HostedZoneId\": \"Z2FDTNDATAQYW2\",",
          "        \"DNSName\": \"api-us.example.com\"",
          "      }",
          "    }",
          "  }, {",
          "    \"Action\": \"CREATE\",",
          "    \"ResourceRecordSet\": {",
          "      \"Name\": \"api.example.com.\",",
          "      \"Type\": \"CNAME\",",
          "      \"SetIdentifier\": \"eu-west-1-secondary\",",
          "      \"Region\": \"eu-west-1\",",
          "      \"HealthCheckId\": \"<eu-west-hc-id>\",",
          "      \"AliasTarget\": {",
          "        \"HostedZoneId\": \"Z2FDTNDATAQYW2\",",
          "        \"DNSName\": \"api-eu.example.com\"",
          "      }",
          "    }",
          "  }]",
          "}",
          "EOF",
          "",
          "aws route53 change-resource-record-sets \\",
          "  --hosted-zone-id Z123YOURZONEID \\",
          "  --change-batch file://routing-policy.json",
        ].join("\n"),
        warning: "Route 53 health checkers run every 30 seconds by default, with failure threshold of 3. So worst-case failover detection is ~90 seconds. For sub-minute failover, set RequestInterval=10 and FailureThreshold=1 (more expensive but faster detection).",
      },
      {
        title: "Step 6: Configure CloudFront with origin failover",
        description: "CloudFront sits in front of both regional API Gateways. Configure primary and failover origins so CloudFront routes to the healthy region automatically — even faster than DNS failover.",
        command: [
          "# Create origin group with primary + failover",
          "cat > distribution-config.json <<EOF",
          "{",
          "  \"Origins\": [{",
          "    \"Id\": \"api-us-east-1\",",
          "    \"DomainName\": \"api-us.example.com\",",
          "    \"CustomOriginConfig\": { \"OriginProtocolPolicy\": \"https-only\" }",
          "  }, {",
          "    \"Id\": \"api-eu-west-1\",",
          "    \"DomainName\": \"api-eu.example.com\",",
          "    \"CustomOriginConfig\": { \"OriginProtocolPolicy\": \"https-only\" }",
          "  }, {",
          "    \"Id\": \"origin-group\",",
          "    \"OriginGroups\": {",
          "      \"Quantity\": 1,",
          "      \"Items\": [{",
          "        \"Id\": \"origin-group\",",
          "        \"Members\": { \"Quantity\": 2, \"Items\": [{ \"OriginId\": \"api-us-east-1\" }, { \"OriginId\": \"api-eu-west-1\" }] },",
          "        \"FailoverCriteria\": { \"StatusCodes\": { \"Quantity\": 5, \"Items\": [500, 502, 503, 504, 404] } }",
          "      }]",
          "    }",
          "  }]",
          "}",
          "EOF",
          "",
          "aws cloudfront create-distribution --distribution-config file://distribution-config.json",
        ].join("\n"),
      },
      {
        title: "Step 7: Set up CI/CD pipeline that deploys to both regions",
        description: "Use CodePipeline with cross-region deploy actions. The pipeline runs in us-east-1 but deploys to both regions in parallel via CodeDeploy.",
        command: [
          "# pipeline.yaml (AWS SAM pipeline definition)",
          "cat > pipeline.yaml <<'EOF'",
          "Resources:",
          "  Pipeline:",
          "    Type: AWS::CodePipeline::Pipeline",
          "    Properties:",
          "      RoleArn: !GetAtt PipelineRole.Arn",
          "      Stages:",
          "        - Name: Source",
          "          Actions:",
          "            - Name: GitHubSource",
          "              ActionTypeId: { Category: Source, Owner: ThirdParty, Provider: GitHub, Version: 1 }",
          "              OutputArtifacts: [{ Name: SourceOutput }]",
          "              Configuration:",
          "                Owner: my-org",
          "                Repo: orders-api",
          "                Branch: main",
          "                OAuthToken: !Ref GitHubToken",
          "        - Name: Build",
          "          Actions:",
          "            - Name: BuildLambda",
          "              ActionTypeId: { Category: Build, Owner: AWS, Provider: CodeBuild, Version: 1 }",
          "              InputArtifacts: [{ Name: SourceOutput }]",
          "              OutputArtifacts: [{ Name: BuildOutput }]",
          "              Configuration:",
          "                ProjectName: !Ref CodeBuildProject",
          "        - Name: DeployUS",
          "          Actions:",
          "            - Name: DeployUSEast1",
          "              ActionTypeId: { Category: Deploy, Owner: AWS, Provider: CloudFormation, Version: 1 }",
          "              InputArtifacts: [{ Name: BuildOutput }]",
          "              Configuration:",
          "                StackName: orders-api-prod",
          "                ActionMode: CREATE_UPDATE",
          "                Region: us-east-1",
          "        - Name: DeployEU",
          "          Actions:",
          "            - Name: DeployEUWest1",
          "              ActionTypeId: { Category: Deploy, Owner: AWS, Provider: CloudFormation, Version: 1 }",
          "              InputArtifacts: [{ Name: BuildOutput }]",
          "              Configuration:",
          "                StackName: orders-api-prod",
          "                ActionMode: CREATE_UPDATE",
          "                Region: eu-west-1",
          "EOF",
        ].join("\n"),
      },
      {
        title: "Step 8: Test regional failover",
        description: "Simulate a regional outage by denying all traffic to your us-east-1 API Gateway. Measure how long until users are routed to eu-west-1. The total RTO should be under 90 seconds (Route 53 health check + DNS TTL).",
        command: [
          "# Step 1: Deny all traffic to us-east-1 API Gateway (simulate outage)",
          "aws apigateway update-stage \\",
          "  --rest-api-id <us-east-api-id> \\",
          "  --stage-name prod \\",
          "  --patch-operations op=replace,path=//*/*/ throttlingBurstLimit,value=0 \\",
          "  --region us-east-1",
          "",
          "# Step 2: Continuously hit the API and watch for failover",
          "while true; do",
          "  curl -sI https://api.example.com/health | head -1",
          "  sleep 5",
          "done",
          "",
          "# Expected: 200 from us-east-1, then 503 for ~30-60s, then 200 from eu-west-1",
          "",
          "# Step 3: Restore the us-east-1 API",
          "aws apigateway update-stage \\",
          "  --rest-api-id <us-east-api-id> \\",
          "  --stage-name prod \\",
          "  --patch-operations op=replace,path=//*/*/ throttlingBurstLimit,value=1000 \\",
          "  --region us-east-1",
        ].join("\n"),
      },
    ],
    troubleshooting: [
      { problem: "DynamoDB Global Tables conflict on concurrent writes", solution: "Use conditional writes (attribute_not_exists) on a deterministic key, OR include a timestamp and use last-writer-wins. For financial transactions, use a single-region-writer pattern where only the primary region accepts writes; secondary regions are read-only until failover." },
      { problem: "Route 53 failover is slow (3+ minutes)", solution: "Lower RequestInterval to 10s and FailureThreshold to 1. Also lower DNS TTL to 30s. This costs more (~$1/month per health check) but enables <60s failover. CloudFront origin failover is faster and should be the primary mechanism." },
      { problem: "Lambda cold starts after failover spike latency", solution: "Enable provisioned concurrency on the secondary region's Lambda functions. Keep at least 5-10 warm instances ready. Use Lambda SnapStart (Java) or smaller runtime (Node.js/Python) for faster cold starts." },
      { problem: "CI/CD pipeline fails in one region but succeeds in the other", solution: "Make deployments independent — don't block the us-east-1 deploy just because eu-west-1 failed. Use CodePipeline manual approval between regions for production deployments." },
      { problem: "Cross-region API calls add latency to the user experience", solution: "CloudFront caches at edge locations globally. For dynamic content, accept that users in distant regions have higher latency — that's the cost of multi-region. Consider Aurora Global Database with read replicas if you need lower-latency cross-region reads." },
    ],
    security: [
      "Use IAM roles per region — don't share credentials across regions.",
      "Enable AWS WAF on CloudFront for global threat protection (single configuration, applies everywhere).",
      "Use ACM certificates in each region — ACM certs are region-scoped. Use the same private key for both regions to allow same-domain HTTPS.",
      "Enable CloudTrail in both regions, aggregate logs to a security account.",
      "Use Secrets Manager with multi-region replicas (or replicate secrets manually) — never store secrets in environment variables in plaintext.",
      "Implement per-region rate limiting on API Gateway to prevent DDoS amplification during failover.",
      "Use AWS Shield Advanced for production multi-region deployments (adds DDoS response team support).",
    ],
    cleanup: [
      "Delete CloudFront distribution (must disable first, wait 15 min for full deletion).",
      "Delete Route 53 health checks and routing records.",
      "Delete both regional CloudFormation stacks: 'aws cloudformation delete-stack --stack-name orders-api-prod --region us-east-1' and same for eu-west-1.",
      "Delete DynamoDB Global Table: 'aws dynamodb delete-global-table --global-table-name Orders'.",
      "Delete the underlying DynamoDB tables in each region.",
      "Delete the CodePipeline and CodeBuild project.",
      "Verify no resources remain — multi-region deployments bill continuously even when 'idle'.",
    ],
    extensions: [
      "Add a third region (ap-southeast-1) for Asia coverage — true global active-active.",
      "Implement Aurora Global Database instead of DynamoDB for relational multi-region.",
      "Add SQS dead-letter queues per region for failed Lambda invocations.",
      "Implement distributed tracing with X-Ray to visualize cross-region request flow.",
      "Add per-region cost allocation tags to compare regional spend.",
      "Build a chaos engineering game day: randomly fail one region monthly.",
      "Add Lambda@Edge for ultra-low-latency auth validation at the edge.",
    ],
  },
  {
    id: "project-7-eks-gitops",
    title: "Project 7: EKS Kubernetes Cluster with ArgoCD GitOps",
    level: "expert",
    estimatedHours: 10,
    cost: "~$200-400/month for the cluster + worker nodes (m5.large x 3)",
    description: "Build a production-grade Amazon EKS (Kubernetes) cluster with GitOps-driven deployments via ArgoCD. This is the architecture used by senior platform engineers at companies running microservices at scale. Master this and you can architect, deploy, and operate any Kubernetes workload on AWS.",
    objectives: [
      "Provision an EKS cluster with managed node groups across 3 AZs",
      "Configure IAM roles for service accounts (IRSA) — least privilege per service",
      "Install ArgoCD for GitOps-based deployments",
      "Set up a Git repository as the single source of truth for cluster state",
      "Implement progressive delivery with Argo Rollouts (canary deployments)",
      "Configure observability with Prometheus + Grafana + Loki",
      "Set up ingress with AWS Load Balancer Controller + ExternalDNS",
      "Implement NetworkPolicies for zero-trust pod-to-pod communication",
    ],
    architecture: {
      nodes: [
        { id: "git", label: "Git Repo (manifests)", type: "source" },
        { id: "argo", label: "ArgoCD", type: "container" },
        { id: "eks", label: "EKS Control Plane", type: "container" },
        { id: "ng1", label: "Node Group AZ-a", type: "container" },
        { id: "ng2", label: "Node Group AZ-b", type: "container" },
        { id: "ng3", label: "Node Group AZ-c", type: "container" },
        { id: "alb", label: "AWS ALB (Ingress)", type: "network" },
        { id: "pods", label: "Microservices (pods)", type: "container" },
        { id: "irsa", label: "IAM Roles for SA", type: "security" },
        { id: "prom", label: "Prometheus + Grafana", type: "observability" },
      ],
      edges: [
        { from: "git", to: "argo", label: "watches" },
        { from: "argo", to: "eks", label: "applies manifests" },
        { from: "eks", to: "ng1" },
        { from: "eks", to: "ng2" },
        { from: "eks", to: "ng3" },
        { from: "alb", to: "pods", label: "routes traffic" },
        { from: "pods", to: "irsa", label: "assumes role" },
        { from: "pods", to: "prom", label: "metrics/logs" },
      ],
    },
    steps: [
      {
        title: "Step 1: Install eksctl and create the cluster",
        description: "eksctl is the official CLI for creating EKS clusters. We'll create a production cluster with managed node groups across 3 AZs, using Graviton (ARM) instances for cost savings.",
        command: [
          "# Install eksctl",
          "curl --silent --location https://github.com/weaveworks/eksctl/releases/latest/download/eksctl_$(uname -s)_amd64.tar.gz | tar xz -C /tmp",
          "sudo mv /tmp/eksctl /usr/local/bin/",
          "",
          "# Create cluster config",
          "cat > cluster.yaml <<'EOF'",
          "apiVersion: eksctl.io/v1alpha5",
          "kind: ClusterConfig",
          "metadata:",
          "  name: prod-cluster",
          "  region: us-east-1",
          "  version: \"1.30\"",
          "managedNodeGroups:",
          "  - name: system-ng",
          "    instanceType: m6g.large    # Graviton (ARM) — 20% cheaper than Intel",
          "    desiredCapacity: 3",
          "    minSize: 3",
          "    maxSize: 6",
          "    availabilityZones: [us-east-1a, us-east-1b, us-east-1c]",
          "    iam:",
          "      withAddonPolicies:",
          "        albIngress: true",
          "        cloudWatch: true",
          "    labels:",
          "      role: system",
          "    taints:",
          "      - key: dedicated",
          "        value: system",
          "        effect: NoSchedule",
          "  - name: app-ng",
          "    instanceType: m6g.large",
          "    desiredCapacity: 6",
          "    minSize: 3",
          "    maxSize: 20",
          "    availabilityZones: [us-east-1a, us-east-1b, us-east-1c]",
          "    iam:",
          "      withAddonPolicies:",
          "        albIngress: true",
          "    labels:",
          "      role: app",
          "EOF",
          "",
          "# Create the cluster (takes 15-20 minutes)",
          "eksctl create cluster -f cluster.yaml",
        ].join("\n"),
        warning: "EKS charges $0.10/hour per cluster (~$73/month) just for the control plane. Worker nodes (EC2) are extra. This is the cost of managed Kubernetes — for small workloads, ECS Fargate is much cheaper. EKS pays off when you have many microservices or need Kubernetes portability.",
      },
      {
        title: "Step 2: Set up IAM Roles for Service Accounts (IRSA)",
        description: "IRSA is the modern way to give Kubernetes pods AWS permissions. Each pod gets a scoped IAM role via OIDC — no shared credentials, true least privilege per service. This is critical for production security.",
        command: [
          "# Create OIDC provider for the cluster (one-time)",
          "eksctl utils associate-iam-oidc-provider \\",
          "  --cluster prod-cluster \\",
          "  --approve",
          "",
          "# Create IAM role for a service called 'orders-api'",
          "# Trust policy allows the orders-api Kubernetes service account to assume it",
          "cat > trust-policy.json <<EOF",
          "{",
          "  \"Version\": \"2012-10-17\",",
          "  \"Statement\": [{",
          "    \"Effect\": \"Allow\",",
          "    \"Principal\": {",
          "      \"Federated\": \"arn:aws:iam::<account>:oidc-provider/oidc.eks.us-east-1.amazonaws.com/id/<OIDC_ID>\"",
          "    },",
          "    \"Action\": \"sts:AssumeRoleWithWebIdentity\",",
          "    \"Condition\": {",
          "      \"StringEquals\": {",
          "        \"oidc.eks.us-east-1.amazonaws.com/id/<OIDC_ID>:sub\": \"system:serviceaccount:default:orders-api-sa\",",
          "        \"oidc.eks.us-east-1.amazonaws.com/id/<OIDC_ID>:aud\": \"sts.amazonaws.com\"",
          "      }",
          "    }",
          "  }]",
          "}",
          "EOF",
          "",
          "aws iam create-role \\",
          "  --role-name orders-api-irsa \\",
          "  --assume-role-policy-document file://trust-policy.json",
          "",
          "# Attach least-privilege policy — only DynamoDB access to one table",
          "cat > orders-policy.json <<EOF",
          "{",
          "  \"Version\": \"2012-10-17\",",
          "  \"Statement\": [{",
          "    \"Effect\": \"Allow\",",
          "    \"Action\": [\"dynamodb:GetItem\", \"dynamodb:PutItem\", \"dynamodb:Query\"],",
          "    \"Resource\": \"arn:aws:dynamodb:us-east-1:<account>:table/Orders\"",
          "  }]",
          "}",
          "EOF",
          "",
          "aws iam put-role-policy \\",
          "  --role-name orders-api-irsa \\",
          "  --policy-name OrdersDB \\",
          "  --policy-document file://orders-policy.json",
          "",
          "# Annotate the Kubernetes service account to use this IAM role",
          "kubectl annotate serviceaccount orders-api-sa \\",
          "  eks.amazonaws.com/role-arn=arn:aws:iam::<account>:role/orders-api-irsa",
        ].join("\n"),
      },
      {
        title: "Step 3: Install ArgoCD for GitOps",
        description: "ArgoCD watches a Git repo and continuously reconciles the cluster state with what's in Git. Any out-of-band change is automatically reverted. This gives you a full audit trail, easy rollback (git revert), and forces all changes through pull requests.",
        command: [
          "# Install ArgoCD",
          "kubectl create namespace argocd",
          "kubectl apply -n argocd \\",
          "  -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml",
          "",
          "# Get the initial admin password",
          "kubectl -n argocd get secret argocd-initial-admin-secret \\",
          "  -o jsonpath=\"{.data.password}\" | base64 -d",
          "",
          "# Port-forward to access UI",
          "kubectl port-forward svc/argocd-server -n argocd 8080:443 &",
          "# Open https://localhost:8080 — login: admin / <password-from-above>",
          "",
          "# Connect ArgoCD to your Git repo",
          "argocd repo add https://github.com/my-org/k8s-manifests.git \\",
          "  --username <git-user> --password <git-token>",
        ].join("\n"),
      },
      {
        title: "Step 4: Define your application manifest in Git",
        description: "Create a Git repository with your Kubernetes manifests. ArgoCD watches this repo and applies any changes. Below is the ArgoCD Application CRD that links the Git path to a Kubernetes namespace.",
        command: [
          "# manifests/orders-api/argocd-app.yaml",
          "cat > argocd-app.yaml <<EOF",
          "apiVersion: argoproj.io/v1alpha1",
          "kind: Application",
          "metadata:",
          "  name: orders-api",
          "  namespace: argocd",
          "spec:",
          "  project: default",
          "  source:",
          "    repoURL: https://github.com/my-org/k8s-manifests.git",
          "    targetRevision: HEAD",
          "    path: manifests/orders-api",
          "  destination:",
          "    server: https://kubernetes.default.svc",
          "    namespace: default",
          "  syncPolicy:",
          "    automated:",
          "      prune: true     # delete resources removed from Git",
          "      selfHeal: true  # revert manual changes",
          "    syncOptions:",
          "      - CreateNamespace=true",
          "EOF",
          "",
          "kubectl apply -f argocd-app.yaml",
          "",
          "# ArgoCD will now continuously sync this app from Git",
          "# Any push to manifests/orders-api/ triggers an update",
        ].join("\n"),
      },
      {
        title: "Step 5: Implement canary deployments with Argo Rollouts",
        description: "Argo Rollouts is a progressive delivery controller that replaces Kubernetes Deployments with Rollouts — supporting canary, blue/green, and automated rollback based on metrics.",
        command: [
          "# Install Argo Rollouts",
          "kubectl create namespace argo-rollouts",
          "kubectl apply -n argo-rollouts \\",
          "  -f https://github.com/argoproj/argo-rollouts/releases/latest/download/install.yaml",
          "",
          "# Define a Rollout (replaces Deployment) with canary strategy",
          "cat > rollout.yaml <<EOF",
          "apiVersion: argoproj.io/v1alpha1",
          "kind: Rollout",
          "metadata:",
          "  name: orders-api",
          "spec:",
          "  replicas: 6",
          "  selector:",
          "    matchLabels:",
          "      app: orders-api",
          "  template:",
          "    metadata:",
          "      labels:",
          "        app: orders-api",
          "    spec:",
          "      serviceAccountName: orders-api-sa",
          "      containers:",
          "      - name: api",
          "        image: <account>.dkr.ecr.us-east-1.amazonaws.com/orders-api:v2.0.0",
          "        ports: [{ containerPort: 3000 }]",
          "        env:",
          "        - name: TABLE_NAME",
          "          value: Orders",
          "  strategy:",
          "    canary:",
          "      steps:",
          "      - setWeight: 10        # 10% traffic to new version",
          "      - pause: { duration: 2m }",
          "      - analysis:            # Check Prometheus metrics",
          "          templates:",
          "          - templateName: success-rate",
          "          args:",
          "          - name: service-name",
          "            value: orders-api",
          "      - setWeight: 30",
          "      - pause: { duration: 5m }",
          "      - setWeight: 60",
          "      - pause: { duration: 5m }",
          "      - setWeight: 100",
          "EOF",
          "kubectl apply -f rollout.yaml",
        ].join("\n"),
      },
      {
        title: "Step 6: Install AWS Load Balancer Controller for ALB ingress",
        description: "The AWS Load Balancer Controller creates AWS Application Load Balancers when you create a Kubernetes Ingress. Combined with ExternalDNS, this auto-creates Route 53 records when you add new services.",
        command: [
          "# Install AWS Load Balancer Controller via Helm",
          "helm repo add eks https://aws.github.io/eks-charts",
          "helm repo update",
          "",
          "helm install aws-load-balancer-controller eks/aws-load-balancer-controller \\",
          "  -n kube-system \\",
          "  --set clusterName=prod-cluster \\",
          "  --set serviceAccount.create=false \\",
          "  --set serviceAccount.name=aws-load-balancer-controller",
          "",
          "# Define an Ingress — AWS ALB is auto-created",
          "cat > ingress.yaml <<EOF",
          "apiVersion: networking.k8s.io/v1",
          "kind: Ingress",
          "metadata:",
          "  name: orders-api-ingress",
          "  annotations:",
          "    kubernetes.io/ingress.class: alb",
          "    alb.ingress.kubernetes.io/scheme: internet-facing",
          "    alb.ingress.kubernetes.io/target-type: ip",
          "    alb.ingress.kubernetes.io/healthcheck-path: /health",
          "    external-dns.alpha.kubernetes.io/hostname: api.example.com",
          "spec:",
          "  rules:",
          "  - host: api.example.com",
          "    http:",
          "      paths:",
          "      - path: /",
          "        pathType: Prefix",
          "        backend:",
          "          service:",
          "            name: orders-api",
          "            port:",
          "              number: 80",
          "EOF",
          "kubectl apply -f ingress.yaml",
          "",
          "# AWS ALB is created automatically, Route 53 record auto-added by ExternalDNS",
        ].join("\n"),
      },
      {
        title: "Step 7: Install Prometheus + Grafana for observability",
        description: "Use the kube-prometheus-stack Helm chart for a full observability stack. Includes Prometheus (metrics), Grafana (dashboards), Loki (logs), and AlertManager (alerts).",
        command: [
          "# Install kube-prometheus-stack",
          "helm repo add prometheus-community https://prometheus-community.github.io/helm-charts",
          "helm repo update",
          "",
          "helm install monitoring prometheus-community/kube-prometheus-stack \\",
          "  --namespace monitoring --create-namespace \\",
          "  --set prometheus.service.type=LoadBalancer \\",
          "  --set grafana.service.type=LoadBalancer \\",
          "  --set grafana.adminPassword=$(openssl rand -hex 16)",
          "",
          "# Get Grafana admin password",
          "kubectl get secret -n monitoring monitoring-grafana \\",
          "  -o jsonpath=\"{.data.admin-password}\" | base64 -d",
          "",
          "# Port-forward to access Grafana",
          "kubectl port-forward -n monitoring svc/monitoring-grafana 3000:80 &",
          "# Open http://localhost:3000 — login with admin / <password>",
        ].join("\n"),
      },
      {
        title: "Step 8: Apply NetworkPolicies for zero-trust pod communication",
        description: "By default, any pod in Kubernetes can talk to any other pod. NetworkPolicies restrict this. The pattern: default-deny all ingress, then explicitly allow specific paths (orders-api → DynamoDB, but NOT orders-api → payments-api).",
        command: [
          "# Default-deny all ingress in the default namespace",
          "cat > default-deny.yaml <<EOF",
          "apiVersion: networking.k8s.io/v1",
          "kind: NetworkPolicy",
          "metadata:",
          "  name: default-deny-ingress",
          "  namespace: default",
          "spec:",
          "  podSelector: {}  # applies to all pods",
          "  policyTypes:",
          "  - Ingress",
          "EOF",
          "kubectl apply -f default-deny.yaml",
          "",
          "# Allow orders-api to receive traffic only from the ALB ingress controller",
          "cat > allow-orders-api.yaml <<EOF",
          "apiVersion: networking.k8s.io/v1",
          "kind: NetworkPolicy",
          "metadata:",
          "  name: orders-api-allow-ingress",
          "  namespace: default",
          "spec:",
          "  podSelector:",
          "    matchLabels:",
          "      app: orders-api",
          "  policyTypes:",
          "  - Ingress",
          "  ingress:",
          "  - from:",
          "    - namespaceSelector:",
          "        matchLabels:",
          "          kubernetes.io/metadata.name: kube-system",
          "      podSelector:",
          "        matchLabels:",
          "          app.kubernetes.io/name: aws-load-balancer-controller",
          "    ports:",
          "    - protocol: TCP",
          "      port: 3000",
          "EOF",
          "kubectl apply -f allow-orders-api.yaml",
        ].join("\n"),
      },
    ],
    troubleshooting: [
      { problem: "Pods stuck in Pending state", solution: "Run 'kubectl describe pod <name>' and check the Events section. Common causes: insufficient CPU/memory (add node groups), no nodes match nodeSelector/affinity, or PersistentVolumeClaims can't be fulfilled." },
      { problem: "IRSA not working — pods can't access AWS services", solution: "Verify the trust policy has the exact service account name and the OIDC audience is 'sts.amazonaws.com'. Check pod logs for 'AccessDenied'. Use 'aws sts get-caller-identity' inside the pod to verify which role it's assuming." },
      { problem: "ArgoCD shows 'Out of Sync' but no changes were made", solution: "This often happens with Helm charts that use 'lookup' functions or server-side apply. Use 'syncOptions: - ServerSideApply=true' in the Application spec, or use 'ignoreDifferences' to skip known benign differences." },
      { problem: "Canary deployment stuck at 10%", solution: "Argo Rollouts is waiting for the analysis to pass. Check the AnalysisRun status: 'kubectl get analysisrun'. If success rate is below threshold, Rollouts will auto-rollback. Tune the threshold or fix the underlying issue in the new version." },
      { problem: "ALB not created after applying Ingress", solution: "Check AWS Load Balancer Controller logs: 'kubectl logs -n kube-system -l app.kubernetes.io/name=aws-load-balancer-controller'. Common causes: missing IAM permissions for the controller, wrong annotation syntax, or no Internet Gateway in the VPC." },
      { problem: "Prometheus not scraping pod metrics", solution: "Verify the pod has the correct annotations: 'prometheus.io/scrape: true', 'prometheus.io/port: 3000', 'prometheus.io/path: /metrics'. Check that your service exposes the metrics port. Use ServiceMonitor CRD for advanced config." },
      { problem: "NetworkPolicy blocks legitimate traffic", solution: "NetworkPolicies are additive — once you have a default-deny, you must explicitly allow every flow. Start by allowing all ingress from the same namespace, then tighten. Use 'kubectl exec' to test connectivity from specific pods." },
    ],
    security: [
      "Use IRSA (IAM Roles for Service Accounts) — never bake AWS credentials into container images.",
      "Enable EKS Secrets encryption with KMS (envelope encryption for Kubernetes secrets).",
      "Apply NetworkPolicies for zero-trust pod-to-pod communication.",
      "Use Pod Security Standards (restricted baseline) to prevent privileged pods.",
      "Enable EKS audit logs to CloudWatch — track every API call to the Kubernetes API server.",
      "Scan container images with ECR scan-on-push + Trivy in CI/CD — reject critical CVEs.",
      "Use AWS PrivateLink for private ECR access from private subnets.",
      "Regularly patch EKS — subscribe to AWS EKS security bulletins.",
      "Use kube-bench to verify CIS Kubernetes Benchmark compliance.",
      "Enable GuardDuty EKS protection for runtime threat detection.",
    ],
    cleanup: [
      "Delete the EKS cluster: 'eksctl delete cluster --name prod-cluster --region us-east-1' (this deletes node groups and ENIs automatically).",
      "Delete CloudFormation stacks created by ALB controller, ExternalDNS, etc.",
      "Delete IAM roles created for IRSA.",
      "Delete the ECR repository (force delete to remove all images).",
      "Delete Route 53 records created by ExternalDNS.",
      "Verify in the AWS console that no EKS-related ENIs, security groups, or ALBs remain — they bill continuously.",
    ],
    extensions: [
      "Add a second EKS cluster in another region with multi-cluster service mesh (Istio or Linkerd).",
      "Implement Karpenter for smarter node autoscaling (replaces Cluster Autoscaler).",
      "Add OpenTelemetry for vendor-neutral distributed tracing.",
      "Set up Backstage developer portal for service catalog.",
      "Implement policy-as-code with OPA Gatekeeper or Kyverno.",
      "Use AWS App Mesh for mTLS between services.",
      "Add Falco for runtime security monitoring in Kubernetes.",
      "Implement Velero for cluster backup and DR.",
    ],
  },
  {
    id: "project-8-realtime-pipeline",
    title: "Project 8: Real-Time Data Pipeline with Kinesis + Lambda + OpenSearch",
    level: "expert",
    estimatedHours: 7,
    cost: "~$100-200/month at moderate throughput",
    description: "Build a real-time data pipeline that ingests millions of events per second, processes them with sub-second latency, and powers live dashboards. This is the architecture behind real-time analytics at companies like Disney+, Tinder, and Discord.",
    objectives: [
      "Set up Kinesis Data Streams for high-throughput event ingestion",
      "Deploy Lambda consumers with batch processing and ParallelizationFactor",
      "Use Kinesis Firehose for delivery to S3 (raw archive) with Lambda transformation",
      "Index events into Amazon OpenSearch for full-text search and analytics",
      "Build real-time dashboards with OpenSearch Dashboards (Kibana)",
      "Implement dead-letter queues and retry strategies",
      "Add OpenSearch alerting for anomaly detection",
      "Optimize for cost: shard count, Lambda memory, batch size",
    ],
    architecture: {
      nodes: [
        { id: "src", label: "Event Sources (apps, IoT, logs)", type: "source" },
        { id: "kds", label: "Kinesis Data Streams", type: "network" },
        { id: "lam", label: "Lambda Consumer (batch)", type: "compute" },
        { id: "fh", label: "Kinesis Firehose", type: "network" },
        { id: "trans", label: "Lambda Transformer", type: "compute" },
        { id: "s3", label: "S3 (raw archive)", type: "storage" },
        { id: "os", label: "OpenSearch", type: "database" },
        { id: "dash", label: "OpenSearch Dashboards", type: "observability" },
        { id: "dlq", label: "SQS DLQ", type: "network" },
      ],
      edges: [
        { from: "src", to: "kds" },
        { from: "kds", to: "lam", label: "batch poll" },
        { from: "kds", to: "fh", label: "fan-out" },
        { from: "fh", to: "trans" },
        { from: "trans", to: "s3", label: "parquet" },
        { from: "trans", to: "os", label: "real-time index" },
        { from: "lam", to: "os", label: "enriched events" },
        { from: "lam", to: "dlq", label: "failures" },
        { from: "dash", to: "os" },
      ],
    },
    steps: [
      {
        title: "Step 1: Create the Kinesis Data Stream",
        description: "Choose shard count based on expected throughput. Each shard handles 1MB/sec input or 2MB/sec output, and 1,000 records/sec. Use on-demand mode for variable traffic (auto-scales) or provisioned for predictable cost.",
        command: [
          "# Create stream (on-demand mode for variable traffic)",
          "aws kinesis create-stream \\",
          "  --stream-name events-stream \\",
          "  --stream-mode-details StreamMode=ON_DEMAND \\",
          "  --region us-east-1",
          "",
          "# Or provisioned mode (cheaper for steady traffic)",
          "# aws kinesis create-stream --stream-name events-stream --shard-count 10",
          "",
          "# Wait for ACTIVE state",
          "aws kinesis wait stream-exists --stream-name events-stream",
          "aws kinesis describe-stream-summary --stream-name events-stream \\",
          "  --query 'StreamDescriptionSummary.StreamStatus'",
        ].join("\n"),
      },
      {
        title: "Step 2: Create the S3 bucket for raw archive",
        description: "Firehose delivers raw events to S3. Use partitioning by event time for efficient Athena queries.",
        command: [
          "aws s3api create-bucket \\",
          "  --bucket events-archive-prod \\",
          "  --region us-east-1",
          "",
          "# Enable versioning for data protection",
          "aws s3api put-bucket-versioning \\",
          "  --bucket events-archive-prod \\",
          "  --versioning-configuration Status=Enabled",
          "",
          "# Lifecycle: move to Glacier after 90 days, delete after 7 years",
          "cat > lifecycle.json <<EOF",
          "{",
          "  \"Rules\": [{",
          "    \"Id\": \"archive-and-expire\",",
          "    \"Status\": \"Enabled\",",
          "    \"Filter\": { \"Prefix\": \"\" },",
          "    \"Transitions\": [",
          "      { \"Days\": 90, \"StorageClass\": \"GLACIER\" },",
          "      { \"Days\": 365, \"StorageClass\": \"DEEP_ARCHIVE\" }",
          "    ],",
          "    \"Expiration\": { \"Days\": 2555 }",
          "  }]",
          "}",
          "EOF",
          "aws s3api put-bucket-lifecycle-configuration \\",
          "  --bucket events-archive-prod \\",
          "  --lifecycle-configuration file://lifecycle.json",
        ].join("\n"),
      },
      {
        title: "Step 3: Deploy the Lambda transformer for Firehose",
        description: "Firehose can invoke a Lambda to transform records before delivering to S3. Common transformations: parse JSON, enrich with lookup data, convert to Parquet via Glue schema, redact PII.",
        command: [
          "# transformer.py — Firehose transformation Lambda",
          "cat > transformer.py <<'PYEOF'",
          "import base64",
          "import json",
          "import urllib.parse",
          "",
          "def lambda_handler(event, context):",
          "    output = []",
          "    for record in event['records']:",
          "        # Decode the base64-encoded record data",
          "        payload = base64.b64decode(record['data']).decode('utf-8')",
          "        try:",
          "            data = json.loads(payload)",
          "            # Enrich: add processing timestamp",
          "            data['processed_at'] = int(__import__('time').time() * 1000)",
          "            # Redact PII (email)",
          "            if 'email' in data:",
          "                data['email_domain'] = data['email'].split('@')[-1]",
          "                del data['email']",
          "            # Re-encode",
          "            output_data = json.dumps(data) + '\\n'",
          "            output_record = {",
          "                'recordId': record['recordId'],",
          "                'result': 'Ok',",
          "                'data': base64.b64encode(output_data.encode('utf-8')).decode('utf-8')",
          "            }",
          "        except Exception as e:",
          "            # Send failed records to DLQ (not the S3 archive)",
          "            output_record = {",
          "                'recordId': record['recordId'],",
          "                'result': 'ProcessingFailed',",
          "                'data': record['data']",
          "            }",
          "        output.append(output_record)",
          "    return {'records': output}",
          "PYEOF",
          "",
          "# Package and deploy",
          "zip transformer.zip transformer.py",
          "aws lambda create-function \\",
          "  --function-name firehose-transformer \\",
          "  --runtime python3.12 \\",
          "  --handler transformer.lambda_handler \\",
          "  --role arn:aws:iam::<account>:role/lambda-execution-role \\",
          "  --zip-file fileb://transformer.zip",
        ].join("\n"),
      },
      {
        title: "Step 4: Create Kinesis Firehose with Lambda transformation",
        description: "Firehose reads from Kinesis Data Streams, transforms via Lambda, and delivers to S3 in Parquet format for efficient Athena queries.",
        command: [
          "aws firehose create-delivery-stream \\",
          "  --delivery-stream-name events-firehose \\",
          "  --delivery-stream-type KinesisStreamAsSource \\",
          "  --kinesis-stream-source-configuration \\",
          "    KinesisStreamARN=arn:aws:kinesis:us-east-1:<account>:stream/events-stream,RoleARN=arn:aws:iam::<account>:role/firehose-role \\",
          "  --extended-s3-configuration \\",
          "    RoleARN=arn:aws:iam::<account>:role/firehose-role,\\",
          "    BucketARN=arn:aws:s3:::events-archive-prod,\\",
          "    Prefix=events/year=!{timestamp:yyyy}/month=!{timestamp:MM}/day=!{timestamp:dd}/,\\",
          "    ErrorOutputPrefix=errors/!{firehose:error-output-type}/year=!{timestamp:yyyy}/month=!{timestamp:MM}/day=!{timestamp:dd}/,\\",
          "    BufferingHints={SizeInMBs=128,IntervalInSeconds=60},\\",
          "    CompressionFormat=SNAPPY,\\",
          "    DataFormatConversionConfiguration={\\",
          "      Enabled=true,\\",
          "      InputFormatConfiguration={Deserializer={OpenXJsonSerDe={}}},\\",
          "      OutputFormatConfiguration={Serializer={ParquetSerDe={Compression=SNAPPY}}},\\",
          "      SchemaConfiguration={DatabaseName=events_db,TableName=events,RoleARN=arn:aws:iam::<account>:role/firehose-glue-role,Region=us-east-1}\\",
          "    },\\",
          "    ProcessingConfiguration={Enabled=true,Processors=[{Type=Lambda,Parameters=[{ParameterName=LambdaArn,ParameterValue=arn:aws:lambda:us-east-1:<account>:function:firehose-transformer}]}]}",
        ].join("\n"),
        warning: "Firehose buffers data up to 128MB or 60 seconds (whichever comes first) before delivering to S3. For real-time OpenSearch indexing, lower these — but each delivery costs an S3 PUT. Tune based on your latency vs cost tradeoff.",
      },
      {
        title: "Step 5: Create the OpenSearch cluster",
        description: "OpenSearch (the AWS fork of Elasticsearch) provides full-text search and analytics. For production: 3+ data nodes across AZs, dedicated master nodes for stability, encryption at rest with KMS.",
        command: [
          "aws opensearch create-domain \\",
          "  --domain-name events-search \\",
          "  --engine-version OpenSearch_2.11 \\",
          "  --cluster-config \\",
          "    InstanceType=r6g.large.search,\\",
          "    InstanceCount=3,\\",
          "    DedicatedMasterEnabled=true,\\",
          "    DedicatedMasterType=r6g.large.search,\\",
          "    DedicatedMasterCount=3,\\",
          "    ZoneAwarenessEnabled=true,\\",
          "    ZoneAwarenessConfig={AvailabilityZoneCount=3} \\",
          "  --ebs-options EBSEnabled=true,VolumeType=gp3,VolumeSize=100 \\",
          "  --encryption-at-rest-options Enabled=true \\",
          "  --node-to-node-encryption-options Enabled=true \\",
          "  --domain-endpoint-options EnforceHTTPS=true \\",
          "  --advanced-security-options Enabled=true,InternalUserDatabaseEnabled=true,MasterUserOptions={MasterUserName=admin,MasterUserPassword=<secure-password>}",
          "",
          "# Wait ~15-20 minutes for cluster creation",
          "aws opensearch describe-domain --domain-name events-search \\",
          "  --query 'DomainStatus.Endpoint'",
        ].join("\n"),
      },
      {
        title: "Step 6: Configure Firehose to also deliver to OpenSearch",
        description: "Firehose can deliver to BOTH S3 (raw archive) and OpenSearch (real-time index) — set up a backup of failed records to S3.",
        command: [
          "# Update Firehose to deliver to OpenSearch",
          "aws firehose update-destination \\",
          "  --delivery-stream-name events-firehose \\",
          "  --current-delivery-stream-version-id 1 \\",
          "  --destinations-id destination-id-000000000001 \\",
          "  --opensearch-destination-configuration \\",
          "    RoleARN=arn:aws:iam::<account>:role/firehose-role,\\",
          "    DomainARN=arn:aws:opensearch:us-east-1:<account>:domain/events-search,\\",
          "    IndexName=events,\\",
          "    TypeName=_doc,\\",
          "    IndexRotationPeriod=NoRotation,\\",
          "    BufferingHints={IntervalInSeconds=60,SizeInMBs=5},\\",
          "    RetryOptions={DurationInSeconds=60},\\",
          "    S3BackupMode=FailedDocumentsOnly,\\",
          "    S3Configuration={RoleARN=...,BucketARN=arn:aws:s3:::events-archive-prod,Prefix=firehose-failed/,BufferingHints={SizeInMBs=5,IntervalInSeconds=300},CompressionType=GZIP}",
        ].join("\n"),
      },
      {
        title: "Step 7: Deploy the Lambda consumer for enrichment",
        description: "A direct Lambda consumer reads from Kinesis Data Streams for low-latency enrichment (lookup joined data from DynamoDB, push to OpenSearch in real-time, trigger alerts).",
        command: [
          "# consumer.py — Kinesis consumer Lambda",
          "cat > consumer.py <<'PYEOF'",
          "import base64",
          "import json",
          "import boto3",
          "import os",
          "",
          "os_client = boto3.client('opensearch', endpoint_url=os.environ['OS_ENDPOINT'])",
          "ddb = boto3.resource('dynamodb')",
          "users_table = ddb.Table('Users')",
          "",
          "def lambda_handler(event, context):",
          "    for record in event['Records']:",
          "        # Kinesis data is base64-encoded",
          "        payload = base64.b64decode(record['kinesis']['data']).decode('utf-8')",
          "        try:",
          "            event_data = json.loads(payload)",
          "            # Enrich: look up user info from DynamoDB",
          "            user = users_table.get_item(Key={'userId': event_data.get('userId', '')}).get('Item', {})",
          "            event_data['user_country'] = user.get('country', 'unknown')",
          "            # Index to OpenSearch in real-time",
          "            os_client.index(",
          "                index='events-realtime',",
          "                id=record['kinesis']['sequenceNumber'],",
          "                body=json.dumps(event_data)",
          "            )",
          "        except Exception as e:",
          "            print(f'Failed: {e}')",
          "            # Send to DLQ for retry",
          "            sqs = boto3.client('sqs')",
          "            sqs.send_message(",
          "                QueueUrl=os.environ['DLQ_URL'],",
          "                MessageBody=payload",
          "            )",
          "    return {'statusCode': 200, 'processed': len(event['Records'])}",
          "PYEOF",
          "",
          "# Create event source mapping (Lambda polls Kinesis)",
          "aws lambda create-event-source-mapping \\",
          "  --function-name events-consumer \\",
          "  --event-source arn:aws:kinesis:us-east-1:<account>:stream/events-stream \\",
          "  --starting-position LATEST \\",
          "  --batch-size 100 \\",
          "  --maximum-batching-window-in-seconds 5 \\",
          "  --parallelization-factor 10 \\",
          "  --maximum-record-age-in-seconds 3600 \\",
          "  --bisect-batch-on-function-error true \\",
          "  --destination-config OnFailure={Destination=arn:aws:sqs:us-east-1:<account>:events-dlq}",
        ].join("\n"),
      },
      {
        title: "Step 8: Build real-time dashboards in OpenSearch Dashboards",
        description: "OpenSearch Dashboards (Kibana fork) lets you build visualizations on top of indexed data. Create time-series charts, geolocation maps, and anomaly detectors.",
        command: [
          "# Access OpenSearch Dashboards (port-forward or via VPN)",
          "aws opensearch describe-domain --domain-name events-search \\",
          "  --query 'DomainStatus.Endpoints' --output text",
          "",
          "# Open https://<endpoint>/_dashboards/ in browser",
          "# Login with admin / <password-from-step-5>",
          "",
          "# Create index pattern: events-* (matches both archive and realtime)",
          "# Then build visualizations:",
          "# 1. Time-series: events per minute (line chart, X: timestamp, Y: count)",
          "# 2. Top event types (pie chart)",
          "# 3. Geomap of user_country field (region map)",
          "# 4. Anomaly detector: alerts when event rate deviates from baseline",
          "",
          "# Set up alerting via Slack webhook",
          "cat > alert-config.json <<EOF",
          "{",
          "  \"name\": \"event-rate-anomaly\",",
          "  \"detector_type\": \"metric\",",
          "  \"detector\": {",
          "    \"description\": \"Alerts on event rate anomalies\",",
          "    \"inputs\": [{",
          "      \"search\": { \"indices\": [\"events-*\"], \"query\": { \"query_string\": { \"query\": \"*\" } } }",
          "    }],",
          "    \"schedule\": { \"period\": { \"interval\": 5, \"unit\": \"Minutes\" } },",
          "    \"triggers\": [{",
          "      \"name\": \"anomaly-detected\",",
          "      \"severity\": \"1\",",
          "      \"condition\": { \"script\": { \"source\": \"ctx.results[0].hits.total.value > 10000\" } },",
          "      \"actions\": [{",
          "        \"name\": \"slack-notify\",",
          "        \"destination_id\": \"<slack-destination-id>\",",
          "        \"message_template\": { \"source\": \"Anomaly detected: {{ctx.results.0.hits.total.value}} events in 5 min\" }",
          "      }]",
          "    }]",
          "  }",
          "}",
          "EOF",
        ].join("\n"),
      },
    ],
    troubleshooting: [
      { problem: "Kinesis throttling on high throughput", solution: "Switch to on-demand mode (auto-scales) or add more shards. Use Kinesis Producer Library (KPL) for batching on the producer side. Monitor 'WriteProvisionedThroughputExceeded' metric." },
      { problem: "Lambda consumer lag growing", solution: "Increase ParallelizationFactor (up to 10 — processes 10 batches per shard concurrently). Reduce batch window (5s instead of 300s). Add more Lambda concurrency. Consider Kinesis Enhanced Fan-Out for 2MB/sec per consumer." },
      { problem: "Firehose falling behind", solution: "Increase Lambda timeout for transformations. Check if Lambda is erroring out (look at 'IteratorAge' metric). Consider Direct PUT instead of KinesisStreamAsSource for higher throughput." },
      { problem: "OpenSearch cluster yellow/red status", solution: "Yellow = some shards not replicated. Red = primary shards unavailable. Check node count (need 3+ for HA), disk space, and shard distribution. Use OpenSearch cluster health API: 'GET _cluster/health'." },
      { problem: "OpenSearch indexing lag", solution: "Increase Firehose buffer size (5MB max for OpenSearch destination). Bulk-indexing is much faster than per-document. Check OpenSearch JVM heap pressure — if >75%, scale up the cluster." },
      { problem: "DLQ accumulating messages", solution: "Inspect DLQ messages for patterns. Common causes: malformed JSON, schema mismatch, missing fields. Deploy a DLQ-processor Lambda that handles retries with exponential backoff." },
      { problem: "OpenSearch cost exploding", solution: "Use UltraWarm/Cold storage tiers for older data. Set up Index State Management (ISM) to roll over indices daily and delete after N days. Consider OpenSearch Serverless for sporadic workloads." },
    ],
    security: [
      "Encrypt Kinesis streams with KMS — data is encrypted at rest by default, but KMS gives you key control.",
      "Enable OpenSearch fine-grained access control — role-based access at index level.",
      "Use VPC OpenSearch — never expose OpenSearch to the public internet.",
      "Implement IAM roles for Firehose and Lambda with least privilege.",
      "Redact PII in the Lambda transformer before indexing.",
      "Enable CloudTrail for all Kinesis/OpenSearch API calls.",
      "Use OpenSearch audit logs for compliance (HIPAA, SOC2).",
      "Rotate OpenSearch master password regularly via Secrets Manager.",
    ],
    cleanup: [
      "Delete the OpenSearch domain (snapshot first if you want to keep data): 'aws opensearch delete-domain --domain-name events-search' (takes ~15 min).",
      "Delete the Kinesis Data Stream: 'aws kinesis delete-stream --stream-name events-stream'.",
      "Delete the Kinesis Firehose: 'aws firehose delete-delivery-stream --delivery-stream-name events-firehose'.",
      "Delete all Lambda functions (transformer, consumer, DLQ-processor).",
      "Delete the SQS DLQ.",
      "Empty and delete the S3 bucket.",
      "Delete the DynamoDB Users table (if created for enrichment).",
      "Delete IAM roles for Firehose/Lambda/OpenSearch.",
      "Verify in the console — OpenSearch domains cost $200-2000+/month and bill continuously.",
    ],
    extensions: [
      "Add Kinesis Data Analytics (Flink) for windowed aggregations.",
      "Use OpenSearch k-NN for vector similarity search (RAG applications).",
      "Implement event sourcing: store every event as the source of truth.",
      "Add AWS Glue crawlers to make the S3 archive queryable via Athena.",
      "Build a real-time ML pipeline: Lambda calls SageMaker for inference on each event.",
      "Add Amazon QuickSight for business intelligence dashboards on top of OpenSearch.",
      "Implement cross-region replication for DR.",
      "Use Kinesis Video Streams for video/image ingestion pipelines.",
    ],
  },
  {
    id: "project-9-ml-inference",
    title: "Project 9: Production ML Inference Pipeline with SageMaker",
    level: "expert",
    estimatedHours: 8,
    cost: "~$300-500/month at moderate inference load",
    description: "Build an end-to-end ML inference pipeline: train a model on SageMaker, deploy it as a real-time endpoint with auto-scaling, and serve predictions via API Gateway. This is the architecture behind production ML systems at companies like Pinterest, Instacart, and Netflix.",
    objectives: [
      "Prepare and upload training data to S3",
      "Train an XGBoost model with SageMaker managed training",
      "Deploy the model as a SageMaker real-time endpoint",
      "Configure auto-scaling based on invocation count",
      "Build an API Gateway + Lambda proxy in front of SageMaker",
      "Implement A/B testing between two model versions",
      "Set up SageMaker Model Monitor for data drift detection",
      "Create a CI/CD pipeline that retrains and deploys automatically",
    ],
    architecture: {
      nodes: [
        { id: "client", label: "Client / App", type: "client" },
        { id: "apigw", label: "API Gateway", type: "edge" },
        { id: "lam", label: "Lambda Proxy", type: "compute" },
        { id: "sm", label: "SageMaker Endpoint", type: "container" },
        { id: "model", label: "Model Container (GPU)", type: "container" },
        { id: "s3train", label: "S3 Training Data", type: "storage" },
        { id: "train", label: "SageMaker Training Job", type: "compute" },
        { id: "monitor", label: "Model Monitor", type: "observability" },
        { id: "pipeline", label: "SageMaker Pipeline (CI/CD)", type: "pipeline" },
      ],
      edges: [
        { from: "client", to: "apigw" },
        { from: "apigw", to: "lam" },
        { from: "lam", to: "sm", label: "InvokeEndpoint" },
        { from: "sm", to: "model" },
        { from: "s3train", to: "train" },
        { from: "train", to: "sm", label: "deploy model" },
        { from: "monitor", to: "sm" },
        { from: "pipeline", to: "train" },
      ],
    },
    steps: [
      {
        title: "Step 1: Prepare and upload training data to S3",
        description: "SageMaker expects training data in S3, typically in CSV or Parquet format. Use train/validation/test splits. The data must be accessible by SageMaker's execution role.",
        command: [
          "# Create S3 bucket for ML artifacts",
          "aws s3api create-bucket --bucket ml-pipeline-prod --region us-east-1",
          "",
          "# Upload training data (assuming local CSV files)",
          "aws s3 cp train.csv s3://ml-pipeline-prod/data/train.csv",
          "aws s3 cp validation.csv s3://ml-pipeline-prod/data/validation.csv",
          "",
          "# Verify",
          "aws s3 ls s3://ml-pipeline-prod/data/",
        ].join("\n"),
      },
      {
        title: "Step 2: Create the SageMaker execution role",
        description: "SageMaker needs an execution role with S3 access (read training data, write model artifacts). Use least privilege — only the buckets it needs.",
        command: [
          "cat > trust-policy.json <<EOF",
          "{",
          "  \"Version\": \"2012-10-17\",",
          "  \"Statement\": [{",
          "    \"Effect\": \"Allow\",",
          "    \"Principal\": { \"Service\": \"sagemaker.amazonaws.com\" },",
          "    \"Action\": \"sts:AssumeRole\"",
          "  }]",
          "}",
          "EOF",
          "",
          "aws iam create-role \\",
          "  --role-name sagemaker-execution-role \\",
          "  --assume-role-policy-document file://trust-policy.json",
          "",
          "# Grant least-privilege access to the ML bucket",
          "cat > sagemaker-policy.json <<EOF",
          "{",
          "  \"Version\": \"2012-10-17\",",
          "  \"Statement\": [{",
          "    \"Effect\": \"Allow\",",
          "    \"Action\": [\"s3:GetObject\", \"s3:PutObject\", \"s3:ListBucket\"],",
          "    \"Resource\": [",
          "      \"arn:aws:s3:::ml-pipeline-prod\",",
          "      \"arn:aws:s3:::ml-pipeline-prod/*\"",
          "    ]",
          "  }]",
          "}",
          "EOF",
          "",
          "aws iam put-role-policy \\",
          "  --role-name sagemaker-execution-role \\",
          "  --policy-name S3MLAccess \\",
          "  --policy-document file://sagemaker-policy.json",
        ].join("\n"),
      },
      {
        title: "Step 3: Train the XGBoost model with SageMaker",
        description: "Use SageMaker's built-in XGBoost container — no need to write training code. For custom models, you'd supply your own Docker image.",
        command: [
          "# train.py (Python script using SageMaker SDK)",
          "cat > train.py <<'PYEOF'",
          "import sagemaker",
          "import boto3",
          "",
          "session = sagemaker.Session()",
          "role = 'arn:aws:iam::<account>:role/sagemaker-execution-role'",
          "",
          "# Use built-in XGBoost container",
          "container = sagemaker.image_uris.retrieve('xgboost', session.boto_region_name, '1.7-1')",
          "",
          "xgb = sagemaker.estimator.Estimator(",
          "    container,",
          "    role,",
          "    instance_count=1,",
          "    instance_type='ml.m5.xlarge',",
          "    output_path='s3://ml-pipeline-prod/models/',",
          "    sagemaker_session=session,",
          ")",
          "",
          "# Hyperparameters",
          "xgb.set_hyperparameters(",
          "    max_depth=5,",
          "    eta=0.2,",
          "    gamma=4,",
          "    min_child_weight=6,",
          "    subsample=0.8,",
          "    objective='binary:logistic',",
          "    num_round=50,",
          ")",
          "",
          "# Specify training and validation data",
          "s3_input_train = sagemaker.inputs.TrainingInput(",
          "    s3_data='s3://ml-pipeline-prod/data/train.csv', content_type='csv'",
          ")",
          "s3_input_validation = sagemaker.inputs.TrainingInput(",
          "    s3_data='s3://ml-pipeline-prod/data/validation.csv', content_type='csv'",
          ")",
          "",
          "# Train!",
          "xgb.fit({'train': s3_input_train, 'validation': s3_input_validation})",
          "print(f'Model artifact: {xgb.model_data}')",
          "PYEOF",
          "",
          "pip install sagemaker",
          "python train.py",
          "# Training takes 5-10 minutes for small data; hours for large",
        ].join("\n"),
      },
      {
        title: "Step 4: Deploy the model as a real-time endpoint",
        description: "Deploy to a SageMaker real-time endpoint. Use ml.t2.medium for dev, ml.c5.xlarge for production. Auto-scaling handles traffic spikes.",
        command: [
          "# deploy.py",
          "cat > deploy.py <<'PYEOF'",
          "import sagemaker",
          "",
          "session = sagemaker.Session()",
          "xgb = sagemaker.estimator.Estimator.attach('<training-job-name>')",
          "",
          "# Deploy to real-time endpoint",
          "predictor = xgb.deploy(",
          "    initial_instance_count=2,        # HA: 2+ instances",
          "    instance_type='ml.c5.xlarge',     # production CPU",
          "    endpoint_name='churn-prediction-prod',",
          "    serializer=sagemaker.serializers.CSVSerializer(),",
          "    deserializer=sagemaker.deserializers.JSONDeserializer(),",
          ")",
          "",
          "# Test the endpoint",
          "result = predictor.predict([[0.5, 0.8, 1.2, 25, 0.3, 0.7, 1.0, 0.5, 0.2, 0.4]])",
          "print(f'Prediction: {result}')",
          "PYEOF",
          "",
          "python deploy.py",
          "",
          "# Verify endpoint is in service",
          "aws sagemaker describe-endpoint \\",
          "  --endpoint-name churn-prediction-prod \\",
          "  --query 'EndpointStatus'",
          "# Returns 'InService' when ready",
        ].join("\n"),
        warning: "SageMaker endpoints bill continuously — ml.c5.xlarge costs $0.952/hour per instance. With 2 instances, that's ~$1400/month for the endpoint alone. Use Serverless Inference or Asynchronous Inference for sporadic traffic to save costs.",
      },
      {
        title: "Step 5: Configure auto-scaling on the endpoint",
        description: "SageMaker endpoints support Application Auto Scaling. Scale based on invocation count per instance — typically target 70% utilization to leave headroom for spikes.",
        command: [
          "# Register the endpoint as scalable",
          "aws application-autoscaling register-scalable-target \\",
          "  --service-namespace sagemaker \\",
          "  --resource-id endpoint/churn-prediction-prod/variant/AllTraffic \\",
          "  --scalable-dimension sagemaker:variant:DesiredInstanceCount \\",
          "  --min-capacity 2 \\",
          "  --max-capacity 10",
          "",
          "# Target tracking: keep ~70% utilization",
          "aws application-autoscaling put-scaling-policy \\",
          "  --policy-name churn-endpoint-scaling \\",
          "  --service-namespace sagemaker \\",
          "  --resource-id endpoint/churn-prediction-prod/variant/AllTraffic \\",
          "  --scalable-dimension sagemaker:variant:DesiredInstanceCount \\",
          "  --policy-type TargetTrackingScaling \\",
          "  --target-tracking-scaling-policy-configuration '{",
          "    \"TargetValue\": 70.0,",
          "    \"CustomizedMetricSpecification\": {",
          "      \"MetricName\": \"InvocationsPerInstance\",",
          "      \"Namespace\": \"AWS/SageMaker\",",
          "      \"Dimensions\": [{\"Name\": \"EndpointName\", \"Value\": \"churn-prediction-prod\"}],",
          "      \"Statistic\": \"Sum\"",
          "    },",
          "    \"ScaleInCooldown\": 300,",
          "    \"ScaleOutCooldown\": 60",
          "  }'",
        ].join("\n"),
      },
      {
        title: "Step 6: Build API Gateway + Lambda proxy",
        description: "Don't expose SageMaker endpoints directly to clients — use API Gateway + Lambda as a proxy. This adds authentication, throttling, request validation, and lets you cache responses.",
        command: [
          "# proxy.py — Lambda proxy in front of SageMaker",
          "cat > proxy.py <<'PYEOF'",
          "import boto3",
          "import json",
          "import os",
          "",
          "runtime = boto3.client('sagemaker-runtime')",
          "ENDPOINT = os.environ['ENDPOINT_NAME']",
          "",
          "def lambda_handler(event, context):",
          "    try:",
          "        # Parse request body",
          "        body = json.loads(event.get('body', '{}'))",
          "        features = body.get('features', [])",
          "        # Convert to CSV for XGBoost",
          "        csv_data = ','.join(map(str, features))",
          "        ",
          "        # Invoke SageMaker endpoint",
          "        response = runtime.invoke_endpoint(",
          "            EndpointName=ENDPOINT,",
          "            ContentType='text/csv',",
          "            Body=csv_data",
          "        )",
          "        ",
          "        result = response['Body'].read().decode('utf-8')",
          "        prediction = float(result)",
          "        ",
          "        return {",
          "            'statusCode': 200,",
          "            'headers': {'Content-Type': 'application/json'},",
          "            'body': json.dumps({",
          "                'prediction': prediction,",
          "                'churn_risk': 'high' if prediction > 0.7 else 'low'",
          "            })",
          "        }",
          "    except Exception as e:",
          "        return {",
          "            'statusCode': 500,",
          "            'body': json.dumps({'error': str(e)})",
          "        }",
          "PYEOF",
          "",
          "# Package and deploy Lambda",
          "zip proxy.zip proxy.py",
          "aws lambda create-function \\",
          "  --function-name churn-proxy \\",
          "  --runtime python3.12 \\",
          "  --handler proxy.lambda_handler \\",
          "  --role arn:aws:iam::<account>:role/lambda-sagemaker-proxy-role \\",
          "  --zip-file fileb://proxy.zip \\",
          "  --environment Variables={ENDPOINT_NAME=churn-prediction-prod} \\",
          "  --timeout 30",
          "",
          "# Create API Gateway HTTP API (cheaper than REST)",
          "aws apigatewayv2 create-api \\",
          "  --name churn-api \\",
          "  --protocol-type HTTP \\",
          "  --target arn:aws:lambda:us-east-1:<account>:function:churn-proxy",
        ].join("\n"),
      },
      {
        title: "Step 7: Set up A/B testing between model variants",
        description: "SageMaker supports production variants — deploy two model versions, route 90% traffic to current and 10% to new. Compare metrics, then promote or rollback.",
        command: [
          "# Create a second model variant",
          "aws sagemaker create-endpoint-config \\",
          "  --endpoint-config-name churn-prediction-prod-v2 \\",
          "  --production-variants \\",
          "    VariantName=v1,ModelName=churn-model-v1,InstanceType=ml.c5.xlarge,InitialVariantWeight=9,InitialInstanceCount=2 \\",
          "    VariantName=v2,ModelName=churn-model-v2,InstanceType=ml.c5.xlarge,InitialVariantWeight=1,InitialInstanceCount=1",
          "",
          "# Update endpoint to use the new config",
          "aws sagemaker update-endpoint \\",
          "  --endpoint-name churn-prediction-prod \\",
          "  --endpoint-config-name churn-prediction-prod-v2",
          "",
          "# Monitor both variants' metrics",
          "aws cloudwatch get-metric-statistics \\",
          "  --namespace AWS/SageMaker \\",
          "  --metric-name Invocations \\",
          "  --dimensions Name=EndpointName,Value=churn-prediction-prod Name=VariantName,Value=v1 \\",
          "  --start-time $(date -d '1 hour ago' -u +%Y-%m-%dT%H:%M:%S) \\",
          "  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \\",
          "  --period 300 --statistics Sum",
        ].join("\n"),
      },
      {
        title: "Step 8: Enable SageMaker Model Monitor for drift detection",
        description: "Model Monitor compares live inference traffic to the training data baseline. If features start to drift (e.g., a feature that was always 0-1 is now 0-1000), it alerts you to retrain.",
        command: [
          "# Create a baseline from the training data",
          "aws sagemaker create-monitoring-schedule \\",
          "  --monitoring-schedule-name churn-monitor \\",
          "  --endpoint-name churn-prediction-prod \\",
          "  --monitoring-schedule-config '{",
          "    \"MonitoringScheduleConfig\": {",
          "      \"ScheduleConfig\": {\"ScheduleExpression\": \"rate(1 hour)\"},",
          "      \"MonitoringJobDefinition\": {",
          "        \"BaselineConfig\": {",
          "          \"BaseliningStrategy\": \"BaseliningFromTrainingData\",",
          "          \"ConstraintsResource\": {\"S3Uri\": \"s3://ml-pipeline-prod/baseline/constraints.json\"},",
          "          \"StatisticsResource\": {\"S3Uri\": \"s3://ml-pipeline-prod/baseline/stats.json\"}",
          "        },",
          "        \"MonitoringInputs\": [{",
          "          \"EndpointInput\": {",
          "            \"EndpointName\": \"churn-prediction-prod\",",
          "            \"LocalPath\": \"/opt/ml/processing/input\"",
          "          }",
          "        }],",
          "        \"MonitoringOutputConfig\": {",
          "          \"MonitoringOutputs\": [{",
          "            \"S3Output\": {\"S3Uri\": \"s3://ml-pipeline-prod/monitoring/\", \"LocalPath\": \"/opt/ml/processing/output\", \"S3UploadMode\": \"Continuous\"}",
          "          }],",
          "          \"KmsKeyId\": \"alias/aws/sagemaker\"",
          "        },",
          "        \"MonitoringResources\": {\"ClusterConfig\": {\"InstanceType\": \"ml.m5.xlarge\", \"InstanceCount\": 1, \"VolumeSizeInGB\": 50}},",
          "        \"MonitoringAppSpecification\": {\"ImageUri\": \"123456789012.dkr.ecr.us-east-1.amazonaws.com/sagemaker-model-monitor-analyzer\"}",
          "      }",
          "    }",
          "  }'",
        ].join("\n"),
      },
    ],
    troubleshooting: [
      { problem: "Training job fails with 'ResourceLimitExceeded'", solution: "Your account's service quota for the instance type is too low. Request a quota increase via AWS console. ml.p3/p4 instances (GPU) often need explicit quota increases." },
      { problem: "Endpoint invocation returns 4xx with 'ModelError'", solution: "The model container failed to handle the request. Check SageMaker logs in CloudWatch Logs: '/aws/sagemaker/Endpoints/churn-prediction-prod'. Common cause: wrong input format (CSV vs JSON), missing features, or out-of-range values." },
      { problem: "Endpoint latency is too high (seconds)", solution: "Right-size the instance type (try GPU for ML models). Batch requests (SageMaker supports up to 100 inferences per call). Use SageMaker Serverless Inference for cold-start optimization. For very low latency (<50ms), use compiled models with Neo." },
      { problem: "Model accuracy degrades in production", solution: "This is data drift — the production data differs from training data. Enable Model Monitor to detect it. Schedule regular retraining. Use SageMaker Pipelines to automate retrain + deploy when drift exceeds threshold." },
      { problem: "SageMaker endpoint costs are exploding", solution: "Use SageMaker Serverless Inference for sporadic traffic (scales to zero). Use Spot Training for training jobs (90% cheaper). Right-size instances. Use SageMaker Savings Plans for steady-state endpoints (50%+ discount)." },
      { problem: "Lambda proxy hitting timeout", solution: "Lambda has 15-min max timeout. SageMaker endpoint invocations can take 6s+. Increase Lambda timeout to 30s+. Set up API Gateway timeout to match. Use SageMaker Asynchronous Inference for long-running predictions." },
      { problem: "A/B test results are inconclusive", solution: "Increase traffic to the new variant (10% → 25% → 50%). Run for longer (at least 1-2 weeks of statistically significant data). Use SageMaker Pipelines +Clarify for bias detection in the new model." },
    ],
    security: [
      "Use SageMaker execution roles with least privilege — only S3 buckets it needs.",
      "Encrypt training data and model artifacts with KMS.",
      "Use VPC mode for SageMaker endpoints — never expose to public internet.",
      "Store API Gateway auth tokens in Secrets Manager.",
      "Enable SageMaker Model Monitor to detect adversarial inputs.",
      "Use SageMaker Clarify for bias detection in models (compliance).",
      "Audit all SageMaker API calls via CloudTrail.",
      "Restrict ECR access if using custom containers.",
      "Network isolate SageMaker notebooks from the internet.",
      "Enable Amazon Macie to detect PII in training data.",
    ],
    cleanup: [
      "Delete the SageMaker endpoint (most expensive): 'aws sagemaker delete-endpoint --endpoint-name churn-prediction-prod'.",
      "Delete the endpoint config: 'aws sagemaker delete-endpoint-config --endpoint-config-name churn-prediction-prod-v2'.",
      "Delete the model: 'aws sagemaker delete-model --model-name churn-model-v1'.",
      "Delete the monitoring schedule: 'aws sagemaker delete-monitoring-schedule --monitoring-schedule-name churn-monitor'.",
      "Delete the API Gateway and Lambda proxy.",
      "Empty and delete the S3 bucket (model artifacts, training data, monitoring output).",
      "Delete IAM roles and policies.",
      "Verify in SageMaker console — endpoints bill continuously until deleted.",
    ],
    extensions: [
      "Use SageMaker Feature Store for shared features across teams.",
      "Implement SageMaker Pipelines for end-to-end MLOps (data prep → train → evaluate → deploy).",
      "Add SageMaker Model Registry for versioned model catalog.",
      "Use SageMaker Edge for deploying models to IoT devices.",
      "Implement Reinforcement Learning with SageMaker RL.",
      "Add human-in-the-loop with SageMaker Ground Truth for labeling.",
      "Use SageMaker Autopilot for automated model training.",
      "Deploy models to AWS Lambda for low-throughput, low-cost inference (small models only).",
    ],
  },
  {
    id: "project-10-event-driven-microservices",
    title: "Project 10: Event-Driven Microservices with EventBridge + SQS + Lambda",
    level: "expert",
    estimatedHours: 9,
    cost: "~$50-120/month at moderate event volume",
    description: "Build a fully decoupled event-driven microservices architecture where services communicate only through events. This is the modern pattern that powers companies like Segment, Zapier, and Enterprise SaaS platforms — enabling independent scaling, deployment, and team ownership.",
    objectives: [
      "Design an event-driven architecture with EventBridge as the event bus",
      "Implement the Saga pattern for distributed transactions",
      "Use SQS for buffering, retries, and dead-letter queues",
      "Build idempotent consumers with DynamoDB as the deduplication store",
      "Implement the Outbox pattern for reliable event publishing",
      "Use EventBridge Schema Registry for event discovery",
      "Add distributed tracing with X-Ray across all services",
      "Implement circuit breakers with Lambda + Step Functions for failure isolation",
    ],
    architecture: {
      nodes: [
        { id: "client", label: "Client / API", type: "client" },
        { id: "orders", label: "Orders Service (Lambda)", type: "compute" },
        { id: "outbox", label: "Outbox Table (DynamoDB)", type: "database" },
        { id: "eb", label: "EventBridge Bus", type: "network" },
        { id: "sqs1", label: "Payment Queue (SQS)", type: "network" },
        { id: "sqs2", label: "Inventory Queue (SQS)", type: "network" },
        { id: "sqs3", label: "Notification Queue (SQS)", type: "network" },
        { id: "pay", label: "Payment Service", type: "compute" },
        { id: "inv", label: "Inventory Service", type: "compute" },
        { id: "notif", label: "Notification Service", type: "compute" },
        { id: "saga", label: "Step Functions Saga Orchestrator", type: "pipeline" },
        { id: "xray", label: "X-Ray (distributed tracing)", type: "observability" },
      ],
      edges: [
        { from: "client", to: "orders" },
        { from: "orders", to: "outbox", label: "write + event" },
        { from: "outbox", to: "eb", label: "DynamoDB Streams → EventBridge" },
        { from: "eb", to: "sqs1" },
        { from: "eb", to: "sqs2" },
        { from: "eb", to: "sqs3" },
        { from: "sqs1", to: "pay" },
        { from: "sqs2", to: "inv" },
        { from: "sqs3", to: "notif" },
        { from: "saga", to: "orders", label: "orchestrates" },
        { from: "saga", to: "pay", label: "compensates on failure" },
        { from: "xray", to: "pay", label: "traces" },
      ],
    },
    steps: [
      {
        title: "Step 1: Create the EventBridge custom event bus",
        description: "EventBridge is the central event router. Create a custom bus (not the default) so you can apply resource-based policies and per-bus permissions.",
        command: [
          "aws events create-event-bus \\",
          "  --name orders-bus \\",
          "  --region us-east-1",
          "",
          "# Set resource policy (only allow services in your account to put events)",
          "cat > bus-policy.json <<EOF",
          "{",
          "  \"Version\": \"2012-10-17\",",
          "  \"Statement\": [{",
          "    \"Effect\": \"Allow\",",
          "    \"Principal\": { \"AWS\": \"arn:aws:iam::<account>:root\" },",
          "    \"Action\": \"events:PutEvents\",",
          "    \"Resource\": \"arn:aws:events:us-east-1:<account>:event-bus/orders-bus\"",
          "  }]",
          "}",
          "EOF",
          "",
          "aws events put-resource-policy \\",
          "  --event-bus-name orders-bus \\",
          "  --policy file://bus-policy.json",
        ].join("\n"),
      },
      {
        title: "Step 2: Implement the Outbox pattern with DynamoDB Streams",
        description: "The Outbox pattern solves the dual-write problem: when you need to update a database AND publish an event, you can't do both atomically. Solution: write the event to a separate Outbox table in the same transaction; DynamoDB Streams publishes it to EventBridge asynchronously.",
        command: [
          "# Create the Orders table with Outbox pattern",
          "aws dynamodb create-table \\",
          "  --table-name Orders \\",
          "  --attribute-definitions AttributeName=orderId,AttributeType=S \\",
          "  --key-schema AttributeName=orderId,KeyType=HASH \\",
          "  --billing-mode PAY_PER_REQUEST \\",
          "  --stream-specification StreamEnabled=true,StreamViewType=NEW_IMAGE",
          "",
          "# Create the Outbox table (stores events to be published)",
          "aws dynamodb create-table \\",
          "  --table-name EventOutbox \\",
          "  --attribute-definitions AttributeName=event_id,AttributeType=S \\",
          "  --key-schema AttributeName=event_id,KeyType=HASH \\",
          "  --billing-mode PAY_PER_REQUEST \\",
          "  --stream-specification StreamEnabled=true,StreamViewType=NEW_IMAGE",
          "",
          "# Lambda that reads from Outbox stream and publishes to EventBridge",
          "cat > outbox_publisher.py <<'PYEOF'",
          "import boto3",
          "import json",
          "",
          "events = boto3.client('events')",
          "",
          "def lambda_handler(event, context):",
          "    for record in event['Records']:",
          "        if record['eventName'] != 'INSERT':",
          "            continue  # Only publish new events",
          "        ",
          "        outbox_item = record['dynamodb']['NewImage']",
          "        event_data = {",
          "            'event_id': outbox_item['event_id']['S'],",
          "            'event_type': outbox_item['event_type']['S'],",
          "            'aggregate_id': outbox_item['aggregate_id']['S'],",
          "            'payload': json.loads(outbox_item['payload']['S']),",
          "            'occurred_at': outbox_item['occurred_at']['S'],",
          "        }",
          "        ",
          "        # Publish to EventBridge",
          "        response = events.put_events(",
          "            Entries=[{",
          "                'EventBusName': 'orders-bus',",
          "                'Source': 'mycompany.orders',",
          "                'DetailType': event_data['event_type'],",
          "                'Detail': json.dumps(event_data),",
          "            }]",
          "        )",
          "        ",
          "        # Mark as published (idempotent — Lambda may retry)",
          "        # Delete from outbox or set published=true",
          "PYEOF",
          "",
          "# Wire up the Lambda to the DynamoDB stream",
          "aws lambda create-event-source-mapping \\",
          "  --function-name outbox-publisher \\",
          "  --event-source arn:aws:dynamodb:us-east-1:<account>:table/EventOutbox/stream/<timestamp> \\",
          "  --starting-position LATEST \\",
          "  --batch-size 10 \\",
          "  --function-response-types ReportBatchItemFailures",
        ].join("\n"),
      },
      {
        title: "Step 3: Create SQS queues for each consumer service",
        description: "Each downstream service (Payment, Inventory, Notification) gets its own SQS queue. EventBridge routes events to queues based on rules. This decouples producers from consumers — services can be deployed independently.",
        command: [
          "# Create the queues with DLQs",
          "for SVC in payment inventory notification; do",
          "  # DLQ first",
          "  aws sqs create-queue --queue-name ${SVC}-dlq",
          "  DLQ_ARN=$(aws sqs get-queue-attributes --queue-url https://sqs.us-east-1.amazonaws.com/<account>/${SVC}-dlq --attribute-names QueueArn --query 'Attributes.QueueArn' --output text)",
          "  ",
          "  # Main queue with redrive policy",
          "  aws sqs create-queue --queue-name ${SVC}-queue --attributes RedrivePolicy='{\"deadLetterTargetArn\":\"'\"$DLQ_ARN\"'\",\"maxReceiveCount\":\"3\"}'",
          "done",
          "",
          "# Create EventBridge rules to route events to each queue",
          "# Payment service handles OrderCreated and OrderCancelled",
          "aws events put-rule \\",
          "  --name payment-rule \\",
          "  --event-bus-name orders-bus \\",
          "  --event-pattern '{\"source\":[\"mycompany.orders\"],\"detail-type\":[\"OrderCreated\",\"OrderCancelled\"]}'",
          "",
          "aws events put-targets \\",
          "  --rule payment-rule \\",
          "  --event-bus-name orders-bus \\",
          "  --targets '[{\"Id\":\"payment-queue\",\"Arn\":\"arn:aws:sqs:us-east-1:<account>:payment-queue\"}]'",
          "",
          "# Similar for inventory and notification services",
        ].join("\n"),
      },
      {
        title: "Step 4: Implement idempotent consumers",
        description: "SQS at-least-once delivery means consumers may receive the same message twice. Use the event_id from the event as an idempotency key — store it in DynamoDB and skip processing if already seen.",
        command: [
          "# payment_consumer.py — idempotent SQS consumer",
          "cat > payment_consumer.py <<'PYEOF'",
          "import boto3",
          "import json",
          "import os",
          "",
          "dynamodb = boto3.resource('dynamodb')",
          "processed = dynamodb.Table(os.environ['PROCESSED_TABLE'])  # Idempotency store",
          "",
          "def lambda_handler(event, context):",
          "    batch_item_failures = []",
          "    ",
          "    for record in event['Records']:",
          "        try:",
          "            # EventBridge wraps the actual event in a 'detail' field",
          "            sqs_body = json.loads(record['body'])",
          "            event_data = sqs_body['detail']  # EventBridge envelope",
          "            event_id = event_data['event_id']",
          "            ",
          "            # Idempotency check — have we processed this event before?",
          "            try:",
          "                processed.put_item(",
          "                    Item={'event_id': event_id, 'processed_at': int(__import__('time').time())},",
          "                    ConditionExpression='attribute_not_exists(event_id)'",
          "                )",
          "            except dynamodb.meta.client.exceptions.ConditionalCheckFailedException:",
          "                # Already processed — skip (idempotent)",
          "                continue",
          "            ",
          "            # Process the payment",
          "            process_payment(event_data)",
          "            ",
          "        except Exception as e:",
          "            # Report failure for this specific message — others in batch can still succeed",
          "            batch_item_failures.append({'itemIdentifier': record['messageId']})",
          "    ",
          "    return {'batchItemFailures': batch_item_failures}",
          "",
          "def process_payment(event_data):",
          "    # Your business logic here",
          "    order_id = event_data['aggregate_id']",
          "    amount = event_data['payload']['amount']",
          "    # Call Stripe/Payment API...",
          "    print(f'Processed payment for order {order_id}: ${amount}')",
          "PYEOF",
          "",
          "# Wire SQS → Lambda with partial batch response",
          "aws lambda create-event-source-mapping \\",
          "  --function-name payment-consumer \\",
          "  --event-source-arn arn:aws:sqs:us-east-1:<account>:payment-queue \\",
          "  --batch-size 10 \\",
          "  --maximum-batching-window-in-seconds 5 \\",
          "  --function-response-types ReportBatchItemFailures",
        ].join("\n"),
      },
      {
        title: "Step 5: Implement the Saga pattern with Step Functions",
        description: "Distributed transactions are hard. The Saga pattern breaks a multi-step transaction into a sequence of local transactions, each with a compensating action for rollback. Step Functions orchestrates this.",
        command: [
          "# saga.asl.json — Step Functions state machine definition",
          "cat > saga.asl.json <<EOF",
          "{",
          "  \"StartAt\": \"CreateOrder\",",
          "  \"States\": {",
          "    \"CreateOrder\": {",
          "      \"Type\": \"Task\",",
          "      \"Resource\": \"arn:aws:lambda:us-east-1:<account>:function:create-order\",",
          "      \"Next\": \"ProcessPayment\",",
          "      \"Catch\": [{",
          "        \"ErrorEquals\": [\"States.ALL\"],",
          "        \"Next\": \"OrderCreationFailed\"",
          "      }]",
          "    },",
          "    \"ProcessPayment\": {",
          "      \"Type\": \"Task\",",
          "      \"Resource\": \"arn:aws:lambda:us-east-1:<account>:function:process-payment\",",
          "      \"Next\": \"ReserveInventory\",",
          "      \"Catch\": [{",
          "        \"ErrorEquals\": [\"States.ALL\"],",
          "        \"Next\": \"CancelOrder\"  // Compensating action",
          "      }]",
          "    },",
          "    \"ReserveInventory\": {",
          "      \"Type\": \"Task\",",
          "      \"Resource\": \"arn:aws:lambda:us-east-1:<account>:function:reserve-inventory\",",
          "      \"Next\": \"OrderComplete\",",
          "      \"Catch\": [{",
          "        \"ErrorEquals\": [\"States.ALL\"],",
          "        \"Next\": \"RefundPayment\"  // Compensating action",
          "      }]",
          "    },",
          "    \"OrderComplete\": {",
          "      \"Type\": \"Succeed\"",
          "    },",
          "    \"RefundPayment\": {",
          "      \"Type\": \"Task\",",
          "      \"Resource\": \"arn:aws:lambda:us-east-1:<account>:function:refund-payment\",",
          "      \"Next\": \"CancelOrder\"",
          "    },",
          "    \"CancelOrder\": {",
          "      \"Type\": \"Task\",",
          "      \"Resource\": \"arn:aws:lambda:us-east-1:<account>:function:cancel-order\",",
          "      \"Next\": \"OrderFailed\"",
          "    },",
          "    \"OrderCreationFailed\": { \"Type\": \"Fail\", \"Error\": \"OrderCreationFailed\" },",
          "    \"OrderFailed\": { \"Type\": \"Fail\", \"Error\": \"OrderFailed\" }",
          "  }",
          "}",
          "EOF",
          "",
          "aws stepfunctions create-state-machine \\",
          "  --name order-saga \\",
          "  --definition file://saga.asl.json \\",
          "  --role-arn arn:aws:iam::<account>:role/step-functions-role",
        ].join("\n"),
      },
      {
        title: "Step 6: Register event schemas in EventBridge Schema Registry",
        description: "EventBridge Schema Registry auto-discovers event schemas as they flow through the bus. Code bindings generate TypeScript/Python types so consumers always match the producer's schema.",
        command: [
          "# Enable schema discovery on the bus",
          "aws schemas create-discoverer \\",
          "  --source arn:aws:events:us-east-1:<account>:event-bus/orders-bus",
          "# Returns DiscovererId",
          "",
          "aws schemas start-discoverer \\",
          "  --discoverer-id <discoverer-id>",
          "",
          "# After a few events flow through, schemas appear in the registry",
          "aws schemas list-schemas --registry-name discovered-schemas",
          "",
          "# Download code bindings (TypeScript)",
          "aws schemas get-code-binding-source \\",
          "  --registry-name discovered-schemas \\",
          "  --schema-name mycompany-orders-OrderCreated \\",
          "  --language typescript \\",
          "  --schema-version 1 output-dir/",
        ].join("\n"),
      },
      {
        title: "Step 7: Add distributed tracing with X-Ray",
        description: "X-Ray traces requests across all services. The Lambda Powertools library auto-instruments AWS SDK calls and SQS/EventBridge interactions. View traces in the X-Ray console to find bottlenecks.",
        command: [
          "# payment_consumer.py with X-Ray tracing (using Powertools)",
          "cat > payment_consumer_traced.py <<'PYEOF'",
          "from aws_lambda_powertools import Tracer, Logger",
          "from aws_lambda_powertools.utilities.batch import BatchProcessor, EventType",
          "from aws_lambda_powertools.utilities.parser import parse",
          "import boto3",
          "",
          "tracer = Tracer()",
          "logger = Logger()",
          "processor = BatchProcessor(event_type=EventType.SQS)",
          "",
          "dynamodb = boto3.resource('dynamodb')",
          "processed = dynamodb.Table('ProcessedEvents')",
          "",
          "@tracer.capture_method",
          "def process_payment(event_data):",
          "    # This is auto-traced by X-Ray",
          "    order_id = event_data['aggregate_id']",
          "    amount = event_data['payload']['amount']",
          "    logger.info('Processing payment', extra={'order_id': order_id, 'amount': amount})",
          "    # ... actual payment logic",
          "",
          "@logger.inject_lambda_context",
          "def lambda_handler(event, context):",
          "    return processor.process(event, process_payment)",
          "PYEOF",
          "",
          "# Add X-Ray tracing permission to Lambda role",
          "aws iam attach-role-policy \\",
          "  --role-name payment-consumer-role \\",
          "  --policy-arn arn:aws:iam::aws:policy/AWSXRayDaemonWriteAccess",
        ].join("\n"),
      },
      {
        title: "Step 8: Implement circuit breakers for failure isolation",
        description: "When a downstream service fails repeatedly, stop calling it — let it recover. Step Functions + Lambda can implement circuit breakers using DynamoDB to track failure counts.",
        command: [
          "# circuit_breaker.py — Lambda middleware",
          "cat > circuit_breaker.py <<'PYEOF'",
          "import boto3",
          "import json",
          "import time",
          "import os",
          "",
          "dynamodb = boto3.resource('dynamodb')",
          "cb_table = dynamodb.Table(os.environ['CIRCUIT_BREAKER_TABLE'])",
          "",
          "FAILURE_THRESHOLD = 5        # Open after 5 failures",
          "RECOVERY_TIMEOUT = 60        # Try again after 60 seconds",
          "HALF_OPEN_SUCCESS = 3        # Close circuit after 3 successes",
          "",
          "def check_circuit(service_name):",
          "    \"\"\"Returns 'closed' (allow), 'open' (block), or 'half-open' (allow with monitoring).\"\"\"",
          "    response = cb_table.get_item(Key={'service_name': service_name})",
          "    if 'Item' not in response:",
          "        return 'closed'  # No failures yet",
          "    ",
          "    state = response['Item'].get('state', 'closed')",
          "    failure_count = int(response['Item'].get('failure_count', 0))",
          "    last_failure = int(response['Item'].get('last_failure_time', 0))",
          "    ",
          "    if state == 'open':",
          "        if time.time() - last_failure > RECOVERY_TIMEOUT:",
          "            # Half-open: allow one request through",
          "            cb_table.update_item(",
          "                Key={'service_name': service_name},",
          "                UpdateExpression='SET #state = :half',",
          "                ExpressionAttributeNames={'#state': 'state'},",
          "                ExpressionAttributeValues={':half': 'half-open'}",
          "            )",
          "            return 'half-open'",
          "        return 'open'",
          "    return state",
          "",
          "def record_success(service_name):",
          "    response = cb_table.get_item(Key={'service_name': service_name})",
          "    if 'Item' not in response:",
          "        return",
          "    success_count = int(response['Item'].get('success_count', 0)) + 1",
          "    if success_count >= HALF_OPEN_SUCCESS:",
          "        # Close circuit",
          "        cb_table.update_item(",
          "            Key={'service_name': service_name},",
          "            UpdateExpression='SET #state = :closed, failure_count = :zero, success_count = :zero',",
          "            ExpressionAttributeNames={'#state': 'state'},",
          "            ExpressionAttributeValues={':closed': 'closed', ':zero': 0}",
          "        )",
          "    else:",
          "        cb_table.update_item(",
          "            Key={'service_name': service_name},",
          "            UpdateExpression='SET success_count = :count',",
          "            ExpressionAttributeValues={':count': success_count}",
          "        )",
          "",
          "def record_failure(service_name):",
          "    response = cb_table.update_item(",
          "        Key={'service_name': service_name},",
          "        UpdateExpression='ADD failure_count :one SET last_failure_time = :time, #state = IF(failure_count >= :threshold, :open, #state)',",
          "        ExpressionAttributeNames={'#state': 'state'},",
          "        ExpressionAttributeValues={':one': 1, ':time': int(time.time()), ':threshold': FAILURE_THRESHOLD - 1, ':open': 'open'}",
          "    )",
          "PYEOF",
        ].join("\n"),
      },
    ],
    troubleshooting: [
      { problem: "Events are being delivered multiple times", solution: "This is expected — EventBridge + SQS are at-least-once. Make every consumer idempotent using the event_id as a deduplication key in DynamoDB. Enable FIFO SQS queues if you need exactly-once (slight cost premium)." },
      { problem: "Outbox table growing unbounded", solution: "Implement a cleanup Lambda that deletes published events older than N days. Or use DynamoDB TTL — set TTL to 7 days; AWS auto-deletes. Verify the event was published before TTL expiry (use CloudWatch metric on publish failures)." },
      { problem: "Saga transactions getting stuck mid-flow", solution: "Step Functions has built-in timeouts. Set per-state timeouts (e.g., 60 seconds for payment). Add a Catch block that triggers compensating actions. Use Step Functions Express Workflows for high-throughput sagas (cheaper but no guaranteed delivery)." },
      { problem: "DLQ accumulating rapidly", solution: "Inspect DLQ messages for patterns. Common causes: schema mismatch (consumer expects field X but producer sends Y), missing idempotency handling, downstream service is down. Deploy a DLQ-processor Lambda with exponential backoff retry." },
      { problem: "EventBridge rules not matching events", solution: "Verify the event source and detail-type match exactly (case-sensitive). Use the EventBridge test event feature to send a sample event and see which rules match. Enable EventBridge logging to CloudWatch for debugging." },
      { problem: "X-Ray traces are incomplete", solution: "Lambda Powertools auto-instruments AWS SDK calls, but custom code needs @tracer.capture_method. Verify the Lambda role has AWSXRayDaemonWriteAccess. Check X-Ray sampling rules — by default only 5% of requests are traced." },
      { problem: "Circuit breaker never opens on real failures", solution: "Tune FAILURE_THRESHOLD and time window. Track failures per minute, not cumulative. Use CloudWatch alarms to alert when circuit opens — frequent opens indicate a downstream service that needs attention." },
      { problem: "Schema registry has too many similar schemas", solution: "Use explicit schema versioning — bump version when fields change. Mark old schemas as deprecated. Use schema evolution rules: only add optional fields, never remove or rename." },
    ],
    security: [
      "Use EventBridge resource-based policies to restrict who can publish to your bus.",
      "Encrypt SQS queues with KMS — default SQS encryption uses AWS-managed keys; for sensitive data use CMKs.",
      "Use IAM roles per consumer Lambda — each role only sees the queues it needs.",
      "Enable CloudTrail for all EventBridge, SQS, and Step Functions API calls.",
      "Validate event payloads against schemas — reject malformed events at the consumer.",
      "Use VPC endpoints for SQS to keep traffic private.",
      "Implement consumer-side rate limiting to prevent a slow consumer from being overwhelmed.",
      "Use Secrets Manager for any credentials (e.g., Stripe API key for payment service).",
      "Audit event bus access — EventBridge can have many publishers; track who puts what events.",
      "Use SageMaker Clarify for bias detection if any service makes ML inferences.",
    ],
    cleanup: [
      "Delete all SQS queues (including DLQs): 'aws sqs delete-queue --queue-url <url>'.",
      "Delete the EventBridge event bus and all rules: 'aws events delete-event-bus --name orders-bus'.",
      "Delete the Step Functions state machine: 'aws stepfunctions delete-state-machine --state-machine-arn <arn>'.",
      "Delete all Lambda functions (orders, outbox-publisher, payment-consumer, etc.).",
      "Delete DynamoDB tables (Orders, EventOutbox, ProcessedEvents, CircuitBreaker).",
      "Delete the X-Ray group and sampling rules.",
      "Delete IAM roles and policies for each service.",
      "Verify in the console — Step Functions and EventBridge have low cost but accumulate per-request charges.",
    ],
    extensions: [
      "Add a GraphQL API layer (AppSync) that aggregates events from multiple services.",
      "Implement CQRS: separate read models (Materialized Views) updated by event consumers.",
      "Use EventBridge global endpoints for multi-region event bus failover.",
      "Add a stream processing layer (Kinesis Data Analytics) for real-time event aggregation.",
      "Implement event sourcing: store every event as the source of truth; rebuild state from events.",
      "Add OpenTelemetry for vendor-neutral tracing (alternative to X-Ray).",
      "Use AWS App Mesh for service mesh capabilities (mTLS, traffic shifting).",
      "Implement a feature-flag service (e.g., AppConfig) for gradual feature rollouts.",
    ],
  },
];
